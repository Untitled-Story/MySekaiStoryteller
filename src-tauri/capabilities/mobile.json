{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile-player",
  "description": "Mobile shell permissions used by the embedded player",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": ["core:window:allow-set-fullscreen"]
}
